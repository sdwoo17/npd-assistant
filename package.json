{
  "name": "npd-assistant",
  "version": "0.2.0",
  "private": true,
  "description": "DOM integration checks for the NPD Assistant frontend",
  "scripts": {
    "test": "node --test tests/ui_flow.cjs",
    "test:browser": "node --test tests/browser_flow.cjs tests/stories_browser.cjs"
  },
  "devDependencies": {
    "jsdom": "30.0.1",
    "playwright": "1.56.1"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
