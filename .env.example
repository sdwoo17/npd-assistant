# Set these in the server process environment. This file is not auto-loaded.
OPENAI_API_KEY=
OPENAI_MODEL=
# Optional, for an app you manage. Short-lived App Store Connect bearer token.
APP_STORE_CONNECT_TOKEN=
# Runtime data is outside the public repository contents and ignored by git.
NPD_DATA_DIR=runtime
